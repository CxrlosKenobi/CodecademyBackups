import numpy as np

example_array = np.array([24, 16, 30, 10, 12, 28, 38, 2, 4, 36])

example_average = np.average(example_array) # Calculates the Mean/Average of an array
example_median = np.median(example_array) # Calculates the Median of an array 

